<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.cowork.admin.meetingRoom.model.mapper.MeetingRoomMapper">
	
	<!-- 회의실 목록 조회 -->
	<select id="meetingRoomList">
		SELECT MEETING_ROOM_NO, MEETING_ROOM_NM
		FROM MEETING_ROOM
		WHERE COM_NO = #{comNo}
		AND MEETING_ROOM_DEL_FL = 'N'
	</select>

	<!-- 회의실 추가 -->
	<insert id="meetingRoomInsert">
		INSERT INTO MEETING_ROOM VALUES (
			SEQ_MEETING_ROOM.NEXTVAL,
			#{meetingRoomNm},
			#{comNo},
			'N'
		)
	</insert>
	
	<!-- 회의실 삭제 -->
	<update id="meetingRoomDelete">
		UPDATE MEETING_ROOM SET MEETING_ROOM_DEL_FL = 'Y',
			MEETING_ROOM_NM = '삭제된 회의실입니다.'
		WHERE MEETING_ROOM_NO = #{meetingRoomNo} AND COM_NO = #{comNo}
	</update>
	
	<!-- 회의실 수정 -->
	<update id="updateMeetingRoom">
		UPDATE MEETING_ROOM SET MEETING_ROOM_NM = #{meetingRoomNm} WHERE MEETING_ROOM_NO = #{meetingRoomNo} AND MEETING_ROOM_DEL_FL = 'N'
	</update>
</mapper>
